# GitHub Copilot Instructions
Read the `.ai/` directory for system patterns, models, and constraints for InsureFlow. The project uses Next.js 14 Server Components and MongoDB. Strictly adhere to `zod` validations, never omit session checks in API handlers, and always mask PAN/Aadhaar data in UI logic.
